{
    frankenphp
}

:{$PORT:8080} {
    root * /app/public

    encode zstd gzip

    # Serve real files/directories directly; everything else → index.php (Laravel router)
    try_files {path} {path}/ /index.php?{query}

    php_server
}
