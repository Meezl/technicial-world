; PHP runtime overrides for this application.
; .user.ini is honoured by PHP-FPM (Railway / nginx) and lets us bump
; defaults without touching the system php.ini.

upload_max_filesize = 64M
post_max_size = 64M
max_execution_time = 240
max_input_time = 240
